// Override this in production (e.g. GitLab Pages) to point at a public tunnel
// such as an ngrok/Cloudflare URL that exposes your laptop's Flask server.
// Example:
// window.API_BASE_URL = "https://your-tunnel.example.com";
window.API_BASE_URL = "";
